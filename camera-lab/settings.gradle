// Отдельная сборка, намеренно не подключённая к сборке мода: у мода Fabric Loom и свои
// зависимости от Minecraft, а лаборатории нужен обычный Java-проект с окном. Общий у них
// только исходный код ядра камер — он подключается напрямую из ../src/main/java (см.
// build.gradle), поэтому лаборатория всегда гоняет то же самое, что уходит в мод, а не копию.
rootProject.name = 'camera-lab'
