.gradle/
.idea/
*.iml
run/

# Только каталоги сборки в корне. Без ведущей косой черты шаблон совпадает с любой
# папкой с таким именем на любой глубине — и `build/` прятал от git целый пакет мода
# com.tutorialschematic.client.build вместе с BuildRunner.
/build/
/out/

# кэш второй сборки, полгигабайта — в репозитории ему делать нечего
.gradle-codex/

# у камерной лаборатории свой сборочный мусор: скомпилированные классы и вложенный gson
camlab/out/
camlab/lib/

# рабочие заметки, не код: постановка задачи для другой модели и выписки её ответов
CAMERA_PROBLEM.md
ответы нейросетей
